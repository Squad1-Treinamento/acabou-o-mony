services:
  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      - UPSTREAMS=${UPSTREAMS}
      - UPSTREAM_HOST=${UPSTREAM_HOST}
      - UPSTREAM_PORT=${UPSTREAM_PORT}
      - NGINX_TEMPLATE=/etc/nginx/nginx.conf.template
      - NGINX_CONF=/etc/nginx/conf.d/default.conf
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost/healthz >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - edge-net

  ngrok:
    image: ngrok/ngrok:alpine
    depends_on:
      - nginx
    ports:
      - "4040:4040"
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command: ["http", "http://nginx:80"]
    networks:
      - edge-net

  # # Servidor de Teste A
  # servico-a:
  #   image: httpd:latest
  #   entrypoint: /bin/sh -c "echo '<h1>Servidor A Customizado</h1>' > /usr/local/apache2/htdocs/index.html && httpd-foreground"
  #   networks:
  #     - edge-net

  # # Servidor de Teste B
  # servico-b:
  #   image: httpd:latest
  #   entrypoint: /bin/sh -c "echo '<h1>Servidor B Customizado</h1>' > /usr/local/apache2/htdocs/index.html && httpd-foreground"
  #   networks:
  #     - edge-net
  
  # Example backend service (optional)
  # app:
  #   image: nginx:alpine
  #   networks:
  #     - edge-net

networks:
  edge-net:
    driver: bridge
