info:
  name: Payment (LOW-RISK) (DECLINED)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/payments"
  body:
    type: json
    data: |-
      {
        "amount": 10001,
        "currency": "USD",
        "idempotency_key": "{{$randomUUID}}",
        "payment_method": {
          "type": "CARD",
          "masked_card": "411111XXXXXX1111",
          "card_token_id": "tok_visa_123"
        }
      }
  auth:
    type: bearer
    token: "{{apiKey}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
